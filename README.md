# 🎵 YouTube Music Telegram Bot

Современный Telegram бот с Web App для поиска и скачивания музыки с YouTube в стильном черно-зеленом дизайне.

## ✨ Возможности

- 🔍 **Быстрый поиск** музыки на YouTube
- ⬇️ **Скачивание** треков в MP3 формате
- 🎨 **Современный дизайн** в черно-зеленых тонах с неоновыми эффектами
- 📱 **Web App** интеграция с Telegram
- ⚡ **Высокая производительность** благодаря FastAPI
- 🎯 **Простой интерфейс** для удобного использования

## 🏗️ Структура проекта

```
youtube-music-bot/
├── bot/                    # Telegram бот (Python + aiogram)
│   ├── main.py            # Основной файл бота
│   ├── requirements.txt   # Зависимости Python
│   └── .env.example       # Пример конфигурации
├── api/                    # Backend API (FastAPI)
│   ├── main.py            # API сервер
│   └── requirements.txt   # Зависимости Python
└── webapp/                 # Frontend (React)
    └── index.html         # Web App интерфейс
```

## 🚀 Установка и запуск

### Предварительные требования

- Python 3.9+
- Node.js (для разработки frontend, опционально)
- FFmpeg (для конвертации аудио)

### Установка FFmpeg

**Ubuntu/Debian:**
```bash
sudo apt update
sudo apt install ffmpeg
```

**MacOS:**
```bash
brew install ffmpeg
```

**Windows:**
Скачайте с [ffmpeg.org](https://ffmpeg.org/download.html)

### 1. Клонирование репозитория

```bash
git clone <your-repo-url>
cd youtube-music-bot
```

### 2. Настройка Telegram бота

```bash
cd bot

# Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate

# Установите зависимости
pip install -r requirements.txt

# Скопируйте .env.example в .env и заполните данные
cp .env.example .env
nano .env  # или используйте любой редактор
```

В файле `.env` укажите:
```env
BOT_TOKEN=your_telegram_bot_token
WEBAPP_URL=https://your-webapp-url.com
```

Получить токен бота можно у [@BotFather](https://t.me/BotFather) в Telegram.

### 3. Настройка API сервера

```bash
cd ../api

# Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate

# Установите зависимости
pip install -r requirements.txt
```

### 4. Настройка Web App

Отредактируйте файл `webapp/index.html` и измените `API_URL`:

```javascript
const API_URL = 'https://your-api-url.com'; // Замените на ваш API URL
```

### 5. Запуск приложения

**Терминал 1 - API сервер:**
```bash
cd api
python main.py
```
API будет доступен на `http://localhost:8000`

**Терминал 2 - Telegram бот:**
```bash
cd bot
python main.py
```

**Терминал 3 - Web App (для разработки):**
```bash
cd webapp
python -m http.server 8080
```
Web App будет доступен на `http://localhost:8080`

## 🌐 Деплой

### Деплой API (Heroku, Railway, или любой VPS)

1. Загрузите содержимое папки `api/` на сервер
2. Установите зависимости: `pip install -r requirements.txt`
3. Запустите: `uvicorn main:app --host 0.0.0.0 --port 8000`

### Деплой Web App (Vercel, Netlify, GitHub Pages)

1. Загрузите `webapp/index.html` на хостинг
2. Убедитесь, что `API_URL` указывает на ваш развёрнутый API
3. Получите URL вашего Web App

### Деплой Telegram бота (VPS или облачный сервер)

1. Загрузите содержимое папки `bot/` на сервер
2. Создайте `.env` файл с вашими данными
3. Установите зависимости: `pip install -r requirements.txt`
4. Запустите: `python main.py`
5. Для постоянной работы используйте systemd, supervisor или screen

## 🎨 Дизайн

Web App использует современный дизайн с:
- **Шрифты**: Orbitron (заголовки) и Rajdhani (основной текст)
- **Цветовая схема**: Черный фон с неоново-зелёными акцентами
- **Анимации**: Плавные переходы и hover эффекты
- **Адаптивность**: Полностью responsive для всех устройств
- **Эффекты**: Анимированная сетка фона и свечения

## 🔧 Настройка

### Изменение качества аудио

В файле `api/main.py` найдите строку:
```python
'preferredquality': '192',  # Измените на 128, 256, 320
```

### Изменение количества результатов поиска

В файле `webapp/index.html` найдите:
```javascript
max_results: 20  // Измените на нужное значение
```

## 📝 API Endpoints

- `GET /` - Проверка статуса API
- `POST /search` - Поиск музыки
  ```json
  {
    "query": "название песни",
    "max_results": 10
  }
  ```
- `POST /download` - Скачивание трека
  ```json
  {
    "video_id": "YouTube_video_ID",
    "format": "mp3"
  }
  ```
- `GET /download/file/{filename}` - Получение файла
- `GET /info/{video_id}` - Детальная информация о видео

## ⚠️ Важные замечания

1. **YouTube Terms of Service**: Убедитесь, что использование соответствует условиям YouTube
2. **Авторские права**: Используйте только для личных целей и законного контента
3. **Ограничения**: API может иметь лимиты на количество запросов
4. **Хранение файлов**: Файлы автоматически удаляются через 1 час

## 🐛 Решение проблем

### Ошибка "Bot token not found"
- Проверьте файл `.env` в папке `bot/`
- Убедитесь, что токен правильно скопирован от BotFather

### Ошибка "Failed to fetch"
- Проверьте, что API сервер запущен
- Убедитесь, что `API_URL` в `index.html` указывает на правильный адрес
- Проверьте CORS настройки в API

### Ошибка скачивания
- Убедитесь, что FFmpeg установлен
- Проверьте права доступа к папке `downloads/`

## 📄 Лицензия

Этот проект создан в образовательных целях. Используйте ответственно и в соответствии с законами.

## 🤝 Вклад

Предложения и улучшения приветствуются! Создавайте issue или pull request.

## 📧 Контакты

Если у вас есть вопросы, свяжитесь через Telegram или создайте issue.

---

**Создано с ❤️ и Claude AI**
